// Copy this file to secrets.h (gitignored — never commit real
// credentials or a device token) and fill in real values before
// building.
#pragma once

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"
