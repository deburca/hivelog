---
type: release
tags: [hivelog/release]
version: v1.1.0
status: in-progress
date:
---
# Release v1.1.0

## Highlights
- Queen observation history CSV export.
- Correct breadcrumb trail on the queen canonical page.

## Included tasks
```dataview
LIST
FROM "tasks"
WHERE status = "done" AND contains(string(release), "v1.1.0")
```
Targeted (not yet all done):
- [[0001-queen-observation-csv-export]]
- [[0002-breadcrumb-queen-canonical]]

## Schema / update hooks
- None expected. Both targeted tasks are read-only / routing changes with no
  `baseFieldDefinitions()` changes, so **no new `hivelog_update_NNNN` hook**
  should be needed. If that changes, add the hook and note it here.

## Release checklist
- [ ] All target tasks `done`
- [ ] `--group hivelog` PHPUnit suite green
- [ ] `ddev drush updb -y && ddev drush cr` clean on a copy of prod data
- [ ] `hivelog.info.yml` version bumped to 1.1.0
- [ ] `README.md` / `AGENTS.md` updated (document the CSV route)
- [ ] Tag created: `git tag v1.1.0`
