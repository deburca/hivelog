name: Collective
type: module
description: 'Structured hive/apiary context for HiveLog, gathered and served via a credentialed API. Optional; works from manually-logged data alone, richer if nanoprobe sensor data is also present. Pairs with the nexus module for AI-synthesised insights.'
core_version_requirement: ^11
package: Custom
version: '1.8.9'
dependencies:
  - hivelog:hivelog
