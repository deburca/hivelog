name: Collective
type: module
description: 'AI-synthesised apiary insights for HiveLog — the intelligence. Optional; works from manually-logged data alone, richer if nanoprobe sensor data is also present.'
core_version_requirement: ^11
package: Custom
version: '1.0.0'
dependencies:
  - hivelog:hivelog
