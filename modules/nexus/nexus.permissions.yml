# Hive insight permissions. Machine-written only — no add/edit
# permissions at all. Relocated here, unchanged, from `collective` by
# task 0102.
view own hive insight:
  title: 'View own hive insights'
view any hive insight:
  title: 'View any hive insight'
delete own hive insight:
  title: 'Delete own hive insights'
delete any hive insight:
  title: 'Delete any hive insight'

# AI provider config permissions. Deliberately no "add" permission — a
# rare, high-trust, billing-relevant credential reference, provisioned
# by an administrator (administer hivelog), not something every
# beekeeper self-serves. Mirrors collective's `api client` permission
# set exactly.
view own ai provider config:
  title: 'View own AI provider configs'
view any ai provider config:
  title: 'View any AI provider config'
edit own ai provider config:
  title: 'Edit own AI provider configs'
edit any ai provider config:
  title: 'Edit any AI provider config'
delete own ai provider config:
  title: 'Delete own AI provider configs'
delete any ai provider config:
  title: 'Delete any AI provider config'
