hivelog.admin:
  title: 'HiveLog'
  description: 'Manage apiaries, hives, and inspections.'
  route_name: hivelog.dashboard
  menu_name: main
  weight: 10

# One child link per HivelogAppNavBuilder::getAllItems() entry — core's
# own 8 built-ins plus every hook_hivelog_app_nav_items() contribution
# (task 0119). Replaces 8 hand-written entries here plus one each in
# collective.links.menu.yml / nexus.links.menu.yml /
# nanoprobe.links.menu.yml (deleted) — a submodule that wants a
# main-menu entry implements the nav hook only now. See
# Drupal\hivelog\Plugin\Derivative\HivelogMenuLinks.
hivelog.nav_item:
  deriver: 'Drupal\hivelog\Plugin\Derivative\HivelogMenuLinks'
  menu_name: main
  parent: hivelog.admin
